# Configuración de la aplicación - Ejemplo
# Copia este archivo como .env y configura los valores según tu entorno

# =============================================================================
# FLASK
# =============================================================================
# IMPORTANTE: Genera una clave secreta fuerte para producción
# Puedes generarla con: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=dev-key-temporal-CAMBIAR-EN-PRODUCCION

# Entorno de la aplicación (development, production, testing)
FLASK_ENV=development

# =============================================================================
# BASE DE DATOS
# =============================================================================
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=economia_db
DB_PORT=3306

# =============================================================================
# LOGGING
# =============================================================================
# Niveles: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# NOTAS DE SEGURIDAD
# =============================================================================
# 1. NUNCA subas el archivo .env al repositorio
# 2. Para producción:
#    - Genera un SECRET_KEY único y fuerte
#    - Establece credenciales de BD seguras
#    - Configura LOG_LEVEL=WARNING o ERROR
# 3. Asegúrate de que .env está en .gitignore
